<div class="container p-4">
    <div class="row">
        <!--
        {{#each works_at_section}}     
            <div class="col-md-3 p-1">
                <div class="card text-center">
                    <div class="card-header l-0">
                        {{sector_trabajo}}
                    </div>
                    <div class="card-body">
                        <blockquote class="blockquote mb-0">
                            <p>Fecha Trabajada: {{fecha_trabajada}}</p>
                            <p>Horas Trabajadas: {{horas_trabajadas}}</p>
                            <footer class="blockquote-footer">Trabajador: {{nombre}} {{apellido}}</footer>
                        </blockquote>
                    </div>
                </div>
            </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>You dont have a tasks in this section</p>
                <a href="/sector_worker/add">Add a task in this link</a>
            </div>
        </div>
        {{/each}}
-->

        {{#each totalHorasSumadas }}
        <div class="col-md-3 p-1">
            <div class="card text-center">
                <div class="card-header l-0">
                    {{sector_trabajo}}
                </div>
                <div class="card-body">
                    <blockquote class="blockquote mb-0">
                        <p>Horas Trabajadas: {{total_horas_seccion}}</p>
                        
                        <!--<footer class="blockquote-footer">Trabajadores Invo:{{#each ../totalWorkersSection}} <p>{{../this}}</p> {{/each}}</footer>-->
                    </blockquote>
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>No tienes tareas en esta seccion</p>
                <a href="/sector_worker/add">Añade una tarea en este link</a>
            </div>
        </div>
        {{/each}}


        {{#each totalHoras}}
        <div class="col-md-6 center fixed-bottom mx-auto p-2">
            <div class="card text-center">
                <div class="card-body">
                    <h4 class="card-title text-uppercase">
                        Horas Totales: {{suma_horas}}
                    </h4>
                </div>
            </div>
        </div>
        {{else}}
        {{!-- Nothing --}}
        {{/each}}


    </div>
</div>